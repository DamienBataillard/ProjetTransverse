<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MoneyPlug" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Créer le composant pour le Titre de l'appli-->
    <style name="TitleTextStyle">
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">@font/DanburyCaps_PERSONAL_USE_ONLY</item>
        <item name="android:textSize">32sp</item>
    </style>

    <!-- Créer le composant pour le Slogan de l'appli-->
    <style name="SloganTextStyle">
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">@font/BreakingNews</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Créer le composant pour le texte dans le status-->
    <style name="StatusTextStyle">
        <item name="android:textColor">@color/yellow</item>
        <item name="android:fontFamily">Inter</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Créer le composant pour le texte dans la page-->
    <style name="PageTextStyle" parent="StatusTextStyle">
        <item name="android:textSize">20sp</item>
    </style>

    <!-- Créer le composant pour le texte dans la barre d'action-->
    <style name="ActionTextStyle" parent="StatusTextStyle">
        <item name="android:textSize">14sp</item>
    </style>
</resources>